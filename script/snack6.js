/*Stampa il cubo dei primi N numeri,
dove N è un numero indicato dall’utente.*/

let numero = parseInt(prompt("inserisci un numero per ottenerne il cubo"));
let cubo = Math.pow(numero,3);
console.log(cubo);
